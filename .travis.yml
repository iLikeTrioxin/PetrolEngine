dist: FreeBSD
sudo: required
language: cpp
compiler: gcc11
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - xorg-dev

install:
  - sudo pkg install -y vulkan-loader vulkan-headers vulkan-extension-layer vulkan-tools libxinerama libxcursor

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - make
